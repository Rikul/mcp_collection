# python-ast-mcp

Small MCP server that provides a few **Python AST inspection** utilities.

- No execution/import of the provided code
- Uses Python's built-in `ast` module

## Tools

- `list_imports(code)`
- `list_definitions(code)`
- `extract_docstring(code, name="")`

See `usage.md` for examples.
