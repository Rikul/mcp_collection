[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "regex-tools-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp",
]

[project.scripts]
regex-tools-mcp = "regex_tools_mcp.server:main"
