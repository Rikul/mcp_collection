# Current Date/Time MCP Server

## What it does

Returns the current date/time (useful for grounding an LLM that lacks real-time awareness).

## When to use it

- Any workflow that needs “what time is it right now?” without relying on the model.

## Tools

(See `README.md` for the authoritative list.)

## How to run

```bash
uv run current-date-time-mcp
```
