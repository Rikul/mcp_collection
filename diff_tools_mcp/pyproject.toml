[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "diff-tools-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp",
]

[project.scripts]
diff-tools-mcp = "diff_tools_mcp.server:main"
