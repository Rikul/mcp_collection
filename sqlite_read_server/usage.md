# SQLite Read Server MCP

## What it does

Provides read-only access helpers for SQLite databases.

## When to use it

- Let an MCP-enabled LLM inspect/query a SQLite DB safely (read-only).

## Tools

(See `README.md` for the authoritative list.)

## How to run

```bash
uv run sqlite-read-server-mcp
```
